export interface UserInputDTO {
    name: string;
    email: string;
    password: string;
}

export interface LoginInputDTO {
    email: string;
    password: string;
}

export interface InsertUserDTO {
    id: string;
    name: string;
    email: string;
    password: string;
}
